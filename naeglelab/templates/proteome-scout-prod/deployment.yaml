apiVersion: apps/v1
kind: Deployment
metadata:
  name: naeglelab-proteome-scout-prod-app-deployment
  namespace: naeglelab
  labels:
    app: naeglelab-proteome-scout-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: naeglelab-proteome-scout-prod
  template:
    metadata:
      labels:
        app: naeglelab-proteome-scout-prod
    spec:
      containers:
      - name: naeglelab-proteome-scout-prod-container
        image: quay.io/naeglelab/proteome-scout-4:{{ .Values.version.NaeglelabProteomeScout4Test }}
        imagePullPolicy: Always
        env:
        - name: FLASK_APP
          value: "app/__init__.py"
        - name: FLASK_DEBUG
          value: "false"
        - name: SMTP_HOST
          value: "out.mail.virginia.edu"
        - name: SMTP_PORT
          value: "25"
        - name: PROTEOMESCOUT_DATA_DIR
          value: "/Volumes/naegle_lab/ProteomeScout-v4/CURRENT/ProteomeScout_Dataset"
        envFrom:
        - secretRef:
            name: proteome-scout-3-secrets-prod
        ports:
        - containerPort: 5000
        volumeMounts:
        - name: proteomescout-data
          mountPath: /Volumes/naegle_lab/ProteomeScout-v4/CURRENT/ProteomeScout_Dataset
          readOnly: true
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1024Mi"
            cpu: "1000m"
      volumes:
      - name: proteomescout-data
        persistentVolumeClaim:
          claimName: naeglelab-proteome-scout-prod-pvc